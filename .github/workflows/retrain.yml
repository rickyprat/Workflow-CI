name: Retrain Sentiment Model

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  retrain:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install -r MLProject/requirements.txt

      - name: Train Model
        working-directory: MLProject
        run: |
          python modelling.py

      - name: Verify MLflow Output
        run: |
          ls -R MLProject/mlruns

      - name: Upload MLflow Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts
          path: |
            MLProject/mlrunsname: Retrain Sentiment Model

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  retrain:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install -r MLProject/requirements.txt

      - name: Train Model
        working-directory: MLProject
        run: |
          python modelling.py

      - name: Verify MLflow Output
        run: |
          ls -R MLProject/mlruns

      - name: Upload MLflow Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts
          path: |
            MLProject/mlruns
