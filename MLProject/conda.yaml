name: sentiment-env

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip

  - pip:
      - mlflow
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - joblib
